[project]
name = "rag-tax"
version = "0.1.0"
description = "Prototype for a tax-law grounded search engine"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "psycopg[binary]>=3.2,<4.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[dependency-groups]
dev = ["httpx>=0.28.1"]
