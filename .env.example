# OpenAI API 키 (임베딩 + LLM 답변 생성)
OPENAI_API_KEY=

# 법제처 Open API OC 키 (법령 수집 시에만 필요, API 서버 운영에는 불필요)
# 발급: https://open.law.go.kr → 오픈API → OC키 발급
LAW_API_KEY=

# PostgreSQL 연결 문자열 (로컬 개발 시 기본값 사용, docker compose 환경에서는 자동 설정)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rag_tax
